<% provide(:title, @user.name) %>
<div class="container">
	<div class="row">
	  <aside class="span4">
		<section>
		  <h1>
			<%= gravatar_for @user %>
			<%= @user.name %>
		  </h1>
			<%= link_to "Settings", edit_user_path(current_user) %>
		</section>
	  </aside>
	</div>
</div>